{
  "name": "@soma-platform/soma-guide",
  "version": "1.0.0",
  "description": "Adrian assistant widget engine — canonical shareable tour overlay",
  "main": "soma-guide.js",
  "scripts": {
    "build": "npm --prefix ../soma-assist-core run build",
    "test": "npm run build && node --test tests/soma-guide.test.js"
  },
  "files": [
    "soma-guide.js",
    "soma-guide.css"
  ],
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
