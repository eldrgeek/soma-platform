# The repo root tracks node_modules for older packages (3,710 files as of
# 2026-07-22). Not repeating that here — `npm install` restores everything,
# and the only dependency is a devDependency used by the QR verification test.
node_modules/
package-lock.json
demo/dist/
